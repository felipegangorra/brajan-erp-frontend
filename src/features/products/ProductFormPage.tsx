import { Typography } from 'antd';

export function ProductFormPage() {
  return (
    <div>
      <Typography.Title level={2}>Novo produto</Typography.Title>
      <Typography.Paragraph>
        Formulário de cadastro de produto será implementado nesta página.
      </Typography.Paragraph>
    </div>
  );
}